version: '3.1'

services:
  generator:
    image: localhost:5000/gen:ci-14
    #ports:
      #- "8080:80"
  mail:
    image: mailhog/mailhog
    #ports:
     # - "8025:8025"
  integration:
    image: localhost:5000/gen:integration-10
    build: .